const config = {
    "apiBaseURL": import.meta.env.VITE_API_BASE_URL,
    "userCookie": import.meta.env.VITE_USER_COOKIE,
    "accessCookie": import.meta.env.VITE_ACCESS_COOKIE,
    "refreshCookie": import.meta.env.VITE_REFRESH_COOKIE,
    "pageSize": import.meta.env.VITE_PAGE_SIZE,
};

export default config;